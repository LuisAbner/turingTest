@if (User.Identity != null)
{
    @if (User.Identity.IsAuthenticated)
    {
        <div class="btn-group dropstart">
            <button class="btn btn-secondary dropdown-toggle" style="background: #112E40;" type="button"
        data-bs-toggle="dropdown" aria-expanded="false">
                Perfil
            </button>
            <ul class="dropdown-menu">
                <li>
                    <h6 class="dropdown-header" style="color: #112E40;">Bienvenido(a) <span>@User.Identity.Name</span> </h6>
                </li>                 
                <li><a class="dropdown-item" asp-controller="Home" asp-action="SingOff" style="color: #112E40;"><i
                    class="bi bi-box-arrow-left"></i> Cerrar sesión</a></li>
            </ul>
        </div>
    }
}